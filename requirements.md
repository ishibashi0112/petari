# petari 要件定義書

**petari — paste AI chat patches onto your codebase**
**petari — AIチャットの返答を、コードベースにぺたり。**

上記を正式タグラインとする(npm の description・README 冒頭・リポジトリ説明に使用)。
名前は npm / コマンド / リポジトリすべて `petari` で統一し、意味の伝達はタグラインが担う。

チャットベースAI(M365 Copilot Chat 等)の返答をローカルのコードベースに安全に適用するための CLI ツール。
本ドキュメントは Claude Code での実装開始を前提とした引き継ぎ仕様である。

---

## 1. 背景と目的

### 1.1 現状の運用

- 社内では AI 活用がチャットベース(M365 Copilot Chat)に限定されており、Claude Code / Aider のようなエージェント型ツールは使えない。
- コンテキスト送出(行き)は repomix / slnmix(自作、VB.NET `.sln`/`.vbproj` 専用)で解決済み。
  コードベースを 1 ファイルにまとめて AI に渡す運用が確立している。
- AI の返答(帰り)は、修正後ファイルを丸ごと出力またはダウンロードリンクで受け取り、
  **手動で** 差し替え・他エディタでの diff 確認・調整を行っている。ここが最大の工数。
- AI 生成コードの説明資料(処理フロー等)も都度 AI に依頼しており、これも工数となっている。

### 1.2 目的

「帰り」の工程を自動化し、疑似 Claude Code 体験を実現する。

- AI に **規約形式** で変更指示ファイル(changes.md)を出力させる
- 本ツールがそれをパースし、ローカルファイルへ一括適用する
- レビューは Git の未コミット差分として VS Code 上で行う(差分 UI は自作しない)
- 適用の履歴(before/after/指示原本)をプロジェクト内に自動保存する
- 説明資料(変更概要・Mermaid フロー)は規約により毎回自動的に手に入る

### 1.3 スコープ外(Non-Goals)

- AI API の呼び出し(ツールは AI と通信しない。入力はファイル/クリップボードのみ)
- コンテキスト生成(repomix / slnmix の役割。本ツールは依存しない)
- 独自の差分レビュー UI(Git + VS Code に委譲)
- 特定言語への依存(VB.NET / TypeScript / C# 等、テキストファイルなら何でも対象)

---

## 2. 全体フロー

```
[slnmix / repomix] --(コンテキスト + 規約文)--> [AIチャット]
                                                  |
                                     changes.md をダウンロード
                                                  |
[petari] --(パース・検証・適用)--> [ローカルの作業ツリー]
    |                                      |
    +--> .petari/history/ に保存      [VS Code Git差分でレビュー・手修正]
                                                  |
                                              [コミット]
```

- 規約文の single source は本ツール。`petari protocol` が規約文を標準出力に出し、
  slnmix はそれを取り込んで出力末尾に付与、repomix は `--instruction-file-path` で読み込む。

---

## 3. 変更指示ファイル(changes.md)の規約フォーマット

AI に出力させるファイル。1 ファイルで複数ファイル分の変更を運ぶ。

### 3.1 構造

```
## CHANGES

(変更概要 — 必須)
(影響ファイル一覧 — 必須)
(処理フローが変わる場合のみ Mermaid 図 — 任意)

### FILE: src/utils/date.ts (replace)
<<<<<<< SEARCH
old code
=======
new code
>>>>>>> REPLACE

### FILE: src/components/New.tsx (create)
<<<<<<< CONTENT
(ファイル全文)
>>>>>>> END

### FILE: src/legacy/Old.vb (rewrite)
<<<<<<< CONTENT
(ファイル全文)
>>>>>>> END

### FILE: src/legacy/Unused.vb (delete)
```

### 3.2 操作種別

| 操作 | 説明 |
|---|---|
| `replace` | search/replace ブロック。1 ファイルに複数ブロック可 |
| `create` | 新規ファイル作成(全文)。既存ファイルが存在する場合はエラー |
| `rewrite` | 既存ファイルの全文置き換え。対象が存在しない場合はエラー |
| `delete` | ファイル削除。ブロック本文なし |

- rename は delete + create で表現(専用操作なし)。
- 規約文には「ファイルの過半が変わる場合は rewrite を使うこと」
  「search ブロックはファイル内で一意に特定できる範囲を含めること」を明記する。

### 3.3 マーカー構文

- Aider 互換(`<<<<<<< SEARCH` / `=======` / `>>>>>>> REPLACE`)。
  LLM が学習データで大量に見ている形式のため、独自形式より遵守率が高い。
- パーサは **行頭がマーカー行と完全一致** する行のみをマーカーと判定する
  (コード中に似た文字列があっても誤爆しない)。
- `### FILE:` 行のパスはプロジェクトルート相対。操作種別は括弧で必須指定。

### 3.4 CHANGES セクション

- changes.md 冒頭に必須。ツールは内容をパースせず、history に原本ごと保存するのみ。
- これにより「AI に何を指示され、なぜこう変わったか」の説明資料が毎回自動で残る。

### 3.5 寛容パース(v0.5.0)

AI チャットが規約から軽微に逸脱した出力(実運用で観測)を、安全な範囲で自動補正して受理する。

- 2 段構え: まず厳密パースし、成功すればそのまま。失敗した場合のみ寛容パースを試し、
  **構文エラーがゼロになった場合に限り** 採用する(1 件でも残れば破棄し、厳密パースの
  エラーを報告する。曖昧な解釈のまま適用に進まない)。
- 許容する逸脱: マーカー記号の個数ずれ(5〜16 個)とスペース欠落 / `## CHANGES`・
  `### FILE:` の見出しレベルずれ / ブロック外に迷い込んだコードフェンス行(出力全体の
  フェンス包み)。SEARCH・REPLACE・CONTENT の本文内は一切加工しない
  (Markdown ファイル自体を変更対象にしても壊れない)。
- 補正した内容は適用前に全件表示する(何をどう読み替えたかを隠さない)。
- 操作種別の欠落(`### FILE: path` のみ)は推定せずエラーのまま(create/rewrite の
  判別にファイル実在の知識が要り、誤推定の影響が大きいため)。

---

## 4. CLI コマンド体系

パッケージ名・コマンド名は `petari`(確定済み。名前の由来は擬音の「ぺたり」— AI の変更を
ローカルにペタッと貼り付ける、というツールの動作そのもの)。npm の `petari` は 2026-08-08 時点で空きを確認済み。
実装初期に publish して名前を押さえること。

### 4.1 `petari [path]` — 適用(デフォルトコマンド)

入力ソースの優先順位:

1. 引数でファイルパス指定
2. `--clip` : クリップボードから読み取り(RDP 環境でのクリップボード共有経由の利用を想定)
3. 無指定 : ダウンロードフォルダと**プロジェクトルート直下**から `changes*.md` を自動検出
   - **更新時刻が直近 30 分以内** のもののみ対象(古いファイルの誤適用防止)
   - 両方に候補がある場合も含め、複数候補は最新を提示し確認

処理手順:

1. パース(構文エラーは即時失敗)
2. **全ブロックをドライラン検証**(マッチング・パス安全性・操作の整合性)
3. 1 つでも失敗があれば **何も書き込まず** 失敗レポートを出力(§7)
4. 全件成功なら: 対象ファイルを history の `before/` へコピー → 書き換え → `after/` へコピー
5. 結果サマリを表示(ファイルごとの適用ブロック数)、「git diff で確認してください」を案内
6. 入力が自動検出(Downloads / プロジェクト直下)由来の場合、適用成功後に changes.md を
   history へ移動(作業場所を散らかさない)。全件適用済みで履歴を作らなかった場合は削除しない

オプション:

- `--dry-run` : 検証と適用予定一覧・差分プレビューのみ。書き込まない
- `--partial` : 検証失敗ブロックをスキップし、成功分のみ適用(既定は all-or-nothing)
- `--root <dir>` : プロジェクトルートの明示指定
- `--yes` : 確認プロンプトをスキップ

### 4.2 `petari undo [ID]`

- ID 省略時は直近の履歴を巻き戻す。
- manifest の操作種別に基づき正しく逆適用する:
  replace/rewrite → before を書き戻し、create → ファイル削除、delete → before から復元。
- **適用後に手修正されている場合**(現状ファイルのハッシュ ≠ after のハッシュ)は
  「手修正が失われますが戻しますか?」と警告・確認する。

### 4.3 `petari show [ID]`

- ID 省略時は直近の履歴を対象にする。
- 既定は `code --diff <before> <after>` を実行し、VS Code のネイティブ差分ビューを開く。
- 複数ファイルの履歴はファイル一覧を提示して選択させる (`--file <path>` で直接指定)。
- `--mine` : **after vs 現在のファイル** の差分を表示 = 適用後の手修正分だけを可視化。
  「どこが AI の変更で、どこが自分の調整か」を後から説明できるようにする。
- **ブラウザフォールバック** (2026-08-12 追加): VS Code の起動が ENOENT / EINVAL で
  失敗した場合 (未インストール・PATH 未設定・Windows の code.cmd)、自己完結 HTML の
  差分レポートを生成して既定ブラウザで開く (全対象ファイルを 1 ページに)。
  `--browser` で強制、`--no-open` で生成のみ (SSH 等)。git や VS Code のない端末でも
  §5 の履歴スナップショットだけで差分確認できる。
- `--edit` : **スナップショット (before / --mine 時は after) vs 現在の実ファイル**を表示。
  VS Code では右ペイン = 実ファイルなのでそのまま手修正できる。VS Code がなければ
  127.0.0.1 限定の一時 HTTP サーバーを起動し、**同梱の Monaco Editor (VS Code の
  エディタ実装, Microsoft, MIT) の差分エディタ**で編集する — VS Code と同じ
  左右分割・view zone による完全な行整列・シンタックスハイライト・ライブ差分更新・
  Cmd/Ctrl+S 保存。保存は §8 のエンコーディング保全 (無変更行は raw バイト維持) を通す
  (Monaco はモデルの改行を単一 EOL に正規化するが、マージは行テキスト単位のため
  無変更行の実バイトは壊れない)。
  - Monaco は固定バージョンを**ビルド時にパッケージへ同梱** (実行時のダウンロードなし)。
    資産配信は起動時に列挙した同梱ファイルの許可リストへの完全一致のみ
  - 保存の安全弁: 128bit 乱数トークン URL・Host/Origin 検証・CSP (エディタページのみ
    同梱 Monaco ('self') + 自前ブートストラップ (nonce)。外部 CDN は遮断。他ページは JS 不可)・
    パス再検証と symlink 拒否・SHA-256 楽観ロック (競合時 409)・無操作 30 分で自動終了 (§9)。
    JS 無効環境でも素のフォーム POST で保存できる
  - ブラウザ編集の除外: CR のみの改行・NUL を含む行があるファイルとバイナリ
    (エンコーディング判定不能) は閲覧のみ (HTML フォームが内容を破壊するため)

### 4.4 `petari list`

- history の一覧(ID・日時・概要・対象ファイル数・成否)を表示。

### 4.5 `petari protocol`

- 規約文(AI への指示テキスト)を標準出力に出す。
- slnmix / repomix 連携の single source。規約文はツール内に同梱し、バージョン管理する。

### 4.6 `petari init` — プロジェクト初回セットアップの一括実行

各プロジェクトで **1 回だけ** 実行すれば運用開始に必要な準備がすべて整うコマンド。
「初回セットアップの手順を覚えておかなくてよい」体験にすることが目的。

実行内容(対話形式。既存ファイルがある場合は上書きせず確認):

1. `.petari/config.json` の雛形を生成
2. `protocol.md` をプロジェクト直下に生成(`petari protocol` と同じ規約文を書き出す)
3. repomix 連携のセットアップ:
   - `repomix.config.json` が存在する場合 →
     `output.instructionFilePath: "protocol.md"` の追記を提案(y/n)
   - 存在しない場合 → 最小構成の `repomix.config.json` の新規作成を提案(y/n)

     ```json
     {
       "output": {
         "instructionFilePath": "protocol.md"
       }
     }
     ```

   - これにより、以降は普通に `repomix` を実行するだけで規約文入りの出力になる
4. `.gitignore` への `.petari/` 追記を提案(なければ `.gitignore` 新規作成も提案)

補足:

- slnmix を使うプロジェクトでは手順 3 は不要(規約文の取り込みは slnmix 本体に組み込むため)。
  slnmix 側の対応が済むまでの暫定として、slnmix 出力に protocol.md を手動で連結する運用も可。
- `--yes` で全提案に自動同意。
- 規約文がバージョンアップされた場合の再生成は `petari protocol > protocol.md` の再実行、
  または `petari init` の再実行(差分があれば更新を提案)で対応する。

---

## 5. 履歴ディレクトリ仕様

プロジェクトルート直下:

```
.petari/
  config.json
  history/
    2026-08-08_1432/
      changes.md            # AI から受け取った指示ファイルの原本
      manifest.json         # 適用結果メタデータ
      before/<相対パス>     # 適用前のファイル(ディレクトリ構造を維持)
      after/<相対パス>      # 適用直後(手修正前)のファイル
```

- 履歴 ID はタイムスタンプ形式 `YYYY-MM-DD_HHmm`(同分内の衝突は連番サフィックス)。
- manifest.json に最低限含めるもの:
  対象ファイル一覧、各ファイルの操作種別、ブロック数、before/after の SHA-256、
  適用日時、成否、`--partial` 時のスキップ情報、入力ソース(パス/クリップボード)。
- `.petari/` は個人の作業ログのため `.gitignore` 推奨(初回に追記を提案)。

---

## 6. search ブロックのマッチング戦略

本ツールの品質の肝。段階的フォールバックは 2 段のみ(fuzzy マッチは事故のもとなので実装しない):

1. **完全一致**
2. **行末 trim 一致**(各行の trailing whitespace を無視して比較)。
   それでも不一致なら **全行 trim 一致**(インデント無視)。
   全行 trim で一致した場合、REPLACE 側のインデントを元ファイルの実インデントに合わせて自動補正する。

追加ルール:

- search ブロックがファイル内で **複数箇所にマッチした場合は曖昧としてエラー**
  (どの段階のマッチでも同様)。
- 同一ファイル内の複数ブロックは上から順に適用。先行ブロックの適用結果に対して後続をマッチさせる。

### 6.1 適用済み検出(冪等性)

SEARCH がどの段階でも見つからない場合、即失敗にする前に **REPLACE の内容がすでに
ファイル内に存在するか** を追加チェックする。存在すればそのブロックは
「適用済み(skipped)」として **成功扱い** でスキップし、書き込みは行わない。
これにより同じ changes.md を再実行しても失敗しない(冪等)。

- 判定は **REPLACE ブロック全体(複数行まるごと)の一致** のみ。部分一致は不可
  (短い行の偶然一致による誤検出防止)。空・空行のみの REPLACE も判定対象外
- 比較は SEARCH マッチングと同じ 3 段階に加え、**行内の連続空白・タブを 1 個に
  圧縮して比較する第 4 段(ws-collapse)** まで許容する(実運用で確認された
  「コメント前の空白数の差」を吸収するため)。この段はスキップ判定専用で、
  書き換え位置を決める SEARCH マッチングには使わない(書き込みを伴わないため
  誤検出の実害が小さい、という非対称性に基づく)
- 複数箇所に存在しても「適用済み」扱いでよい(曖昧エラーにしない。書き込まないため)
- 同様に、rewrite / create は内容がファイル全文と一致する場合、delete は対象が
  既に存在しない場合に「適用済み」として成功扱いにする
- `failed`(SEARCH も REPLACE も見つからない)が 1 件でもあれば何も書き込まない
  all-or-nothing は維持する。`skipped` は失敗にカウントしない
- 全ブロックが適用済みの場合は書き込み・履歴作成なしで正常終了(exit 0)
- 既知の限界: REPLACE が SEARCH を包含する追記型ブロック(例: SEARCH `A` →
  REPLACE `A` + `B`)は、再実行時に SEARCH が再マッチするため重複適用になり得る
  (適用済み検出は SEARCH 不一致時にのみ働く)

---

## 7. 失敗レポート

検証失敗時、**そのまま AI チャットに貼り返せる形式** のレポートを出力する(再依頼が 1 コピペで済む)。

内容:

- どのファイルのどのブロックが「未発見」「複数一致」「パス不正」等で失敗したか
- 失敗した SEARCH ブロックの引用
- 「上記ブロックを、現在のファイル内容に一致するよう修正して再出力してください」という定型指示文

結果サマリは 3 状態を区別して表示する:

- `適用` : 今回新たに置換した
- `済み` : REPLACE 済みだったためスキップした(§6.1。失敗にカウントしない)
- `失敗` : SEARCH も REPLACE も見つからない(真の不一致)。このとき失敗理由に
  「REPLACE の内容も見つからないため、changes.md の基準スナップショットが
  現在のコードベースとずれている可能性があります」というヒントを添える

`--clip-report` 等でレポートをクリップボードへ直接コピーできると尚良い。

---

## 8. エンコーディングと改行の保全(最重要要件)

対象環境にレガシー VB.NET(Shift_JIS + CRLF)が含まれるため、**ここの破壊は許容されない**。

- 既存ファイルは読み込み時に以下を検出し、書き込み時に **完全に維持** する:
  - 文字エンコーディング(UTF-8 / Shift_JIS 等。BOM 有無を含む)
  - 改行コード(CRLF / LF)
  - 末尾改行の有無
- changes.md 自体は UTF-8 前提で読む。適用時に対象ファイルのエンコーディングへ変換して書き込む。
- Shift_JIS に変換不能な文字が REPLACE 側に含まれる場合は検証段階でエラーにする。
- 新規ファイル(create)の既定は UTF-8 / LF。config で上書き可
  (例: `"newFile": { "encoding": "shift_jis", "eol": "crlf" }`)。
- **テストを最も厚くする領域。** Shift_JIS + CRLF + 末尾改行なし、BOM 付き UTF-8 等の
  組み合わせでラウンドトリップテストを必ず用意する。

---

## 9. 安全性

- パスは **プロジェクトルート相対のみ** 受け付ける。絶対パス・`..` を含むパスは拒否。
- プロジェクトルートの特定: カレントから上方向に **最も近い `.git`** を探索。
  なければカレントをルートとする。`--root` で明示上書き可。
  - ネスト構成(親ディレクトリ配下にメニュー単位の Git リポジトリが並ぶ)を想定した仕様。
    メニューフォルダ内で実行すればそのメニューがルートになる。
- Git 作業ツリーに未コミット変更がある場合は警告し、続行確認する(Git 自体は必須としない)。
- シンボリックリンクの書き換えは対象外(検出したらエラー)。

---

## 10. 設定(.petari/config.json)

```jsonc
{
  "downloadsDir": null,          // null = OS 既定を自動解決
  "newFile": { "encoding": "utf8", "eol": "lf" },
  "historyLimit": null,          // 履歴の保持件数上限(null = 無制限)
  "vscodeCommand": "code"        // show で使うコマンド
}
```

- ダウンロードフォルダの既定解決は **クロスプラットフォーム必須**:
  - macOS / Linux: `~/Downloads` 相当
  - Windows: **Known Folder(レジストリ)から取得**。
    OneDrive リダイレクトで `Downloads` が移動されている社用 PC を確実に想定すること。
- グローバル設定(`~/.config/petari/config.json` 等)とプロジェクト設定のマージ(プロジェクト優先)。

---

## 11. 実行環境・技術方針

- Node.js / TypeScript 製 CLI。npm で配布(`pnpm add -g` / `npx` 実行を想定)。
- 対応 OS: Windows / macOS(Linux も可能な範囲で)。
- 開発者の環境: 会社 Windows(RDP 先の開発端末を含む)+ 私物 Mac の往復が前提。
- エンコーディング変換は iconv-lite 等の実績あるライブラリを使用。
- 依存は最小限に。slnmix / repomix への依存は持たない。
- テスト: Vitest。特に §6 マッチングと §8 エンコーディング保全はケースを厚く。

---

## 12. 運用メモ(実装対象外だが前提として共有)

- リモート運用は社内共有フォルダ上の bare リポジトリ + 各自ローカルへ clone / push / pull
  (既に運用実績あり)。本ツールはリモート構成に関知しない。
- ローカルのみで git init したメニュー単位リポジトリも、後から
  `git remote add origin \\server\repos\xxx.git` で移行可能。
- 共同作業時は「AI 変更の適用ブランチは自分でレビュー・コミットしてから共有」を運用ルールとする。

---

## 13. 未確定事項(実装時に決定)

- `petari init` を独立コマンドにするか、初回 apply 時の自動提案に統合するか
- 失敗レポートのクリップボードコピー(`--clip-report`)の要否
- 履歴の保持件数上限の既定値
- Mermaid 図の history からの取り出しやすさ(将来: `show --changes` で CHANGES を表示 等)

---

## 14. 実装の優先順位(提案)

1. パーサ + マッチング(§3, §6)+ エンコーディング保全(§8)— コア。テスト最厚
2. apply 本体(検証 → 適用 → history 保存)+ 失敗レポート(§4.1, §5, §7)
3. undo / show / list(§4.2–4.4)
4. protocol コマンドと規約文の確定(§4.5)+ slnmix 側の取り込み対応
5. Downloads 自動検出のクロスプラットフォーム対応(§10)
6. init / config 周りの仕上げ
